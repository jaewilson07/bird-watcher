# Data

This folder holds runtime data — snapshots, database files, etc.

Files in `snapshots/` are gitignored (they're photos, not code).

By the end of issue #3 you'll have jpgs piling up here as the bird watcher polls the camera.
By the end of issue #6 you'll have a SQLite database (`birds.db`) with your sightings table.